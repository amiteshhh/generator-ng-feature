(function () {
    'use strict';
    /**
     * @ngdoc object
     * @name <%= moduleName %>
     *
     * @description
     * This is `<%= moduleName %>` module.
     */

    var moduleName = '<%= moduleName %>',
            requires = [];

    angular.module(moduleName, requires);

})();